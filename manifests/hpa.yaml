apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: msbuild
  labels:
    app.kubernetes.io/name: msbuild
    app.kubernetes.io/part-of: msbuild-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: msbuild
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80